# @calculogic/doc-engine

Doc-engine core package extracted from app; host app consumes it via `@calculogic/doc-engine`.

## Public exports

- `ContentProviderRegistry`, `parseContentRef`, `splitNamespace`
- Doc-engine resolution and provider types

## Usage note

Do not deep-import internal files; use the package root export.
