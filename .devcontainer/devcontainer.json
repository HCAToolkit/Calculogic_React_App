{
  "name": "Calculogic React App",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:22",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y ripgrep && npm install && npm run build",
  "forwardPorts": [5173],
  "portsAttributes": {
    "5173": {
      "label": "Vite dev server",
      "onAutoForward": "openBrowser"
    }
  }
}
